var chai = require('chai');
var expect = chai.expect;

describe("hello", function() {
    it("mocha", function() {
        // setTimeout(function() {
        //     expect(1).eql(2);
        //     done();
        // }, 250);
        expect(1).eql(1);
    });
});
